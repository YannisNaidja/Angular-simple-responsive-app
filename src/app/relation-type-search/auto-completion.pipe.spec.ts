import { AutoCompletionPipe } from './auto-completion.pipe';

describe('AutoCompletionPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoCompletionPipe();
    expect(pipe).toBeTruthy();
  });
});
